mkdir -p /tmp/$USER-tmp
HOST=`hostname`

# start tmux if no session exists
#tmux has-session -t $HOST 2>/dev/null
#if [ $? -eq 1 ]; then
	#tmux new -s $HOST \; detach-client
	#tmux rename-window terminal
	#tmux new-window -n monitors cmus
	#tmux splitw -v alsamixer
	#tmux splitw -h htop
	#tmux swap-window -s 2 -t 1
#fi
